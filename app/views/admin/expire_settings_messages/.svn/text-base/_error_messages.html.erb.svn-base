<% if @expire_setting.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@expire_setting.errors.count, "error") %> prohibited this new information from being saved:</h2>

    <ul>
    <% @expire_setting.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
